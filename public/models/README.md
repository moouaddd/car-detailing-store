# Models

Drop the real product scan here as:

    alzara-ceramic-shampoo.glb

Then open `app/components/hero/ProductModel.tsx` and set `USE_PLACEHOLDER = false`.
No other scene changes are required — lighting, tilt, float and shadow all
already target this same model group.
